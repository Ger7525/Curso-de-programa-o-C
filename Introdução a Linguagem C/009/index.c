#include <stdio.h>
int main() {
                    //17. Criando variáveis e lendo caracteres do teclado com a função getch()
char letra;
printf("\tDigite uma letra: ");
letra = getc(stdin ); //Lê um caractere do teclado sem exibi-lo na tela
printf("\n\tA letra digitada foi: %c\n", letra);
   
   
   
   
   
                   
    return 0;
}